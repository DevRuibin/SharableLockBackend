spring:
  application:
    name: gateway
  main:
    allow-circular-references: true
  cloud:
    config:
      name: gateway, services
  config:
    import:
      - optional:configserver:http://spring-config-server:8080